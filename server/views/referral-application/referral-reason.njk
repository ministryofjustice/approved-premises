{% extends "../partials/layout.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "../components/error-messages/macro.njk" import errorMessages %}

{% set pageTitle = applicationName + " - Referral Reason" %}

{% block content %}
  <main class="app-container govuk-body">
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-full">
        <form method="post" action="/referral-application/referral-reason">
          <input type="hidden" name="_csrf" value={{csrfToken}}>

          {{ errorMessages(errors) }}

          {% set otherHtml %}
            {{ govukInput({
              id: "other",
              name: "other",
              type: "text",
              classes: "govuk-!-width-one-third",
              label: {
                text: "Other Reason"
              }
            }) }}
          {% endset -%}

          {{ govukRadios({
              idPrefix: "reason",
              name: "reason",
              hint: {
                html: "<p>An individual's Tier and nDelius risk registrations are used to decide if they are eligible to apply for an AP placement. Robert Brown either does not have a Tier or their Tier and registrations do not meet eligibility requirements.</p><p><a href='#'>Read about Tiering and eligibility</a></p>"
              },
              fieldset: {
                legend: {
                  text: "What is the reason for the referral?",
                  isPageHeading: true,
                  classes: "govuk-fieldset__legend--l"
                }
              },
              errorMessage: ({ text: errors.reason } if errors.reason | length) ,
              items: [
                {
                  value: "likely",
                  text: "Robert Brown does not have a Tier yet, but is likely to be eligible",
                  checked: (reason === "likely")
                },
                {
                  value: "condition",
                  text: "AP is a condition of residence in Robert Brown's community order",
                  checked: (reason === "condition")
                },
                {
                  value: "exception",
                  text: "An exception has been agreed between the community and residential heads",
                  checked: (reason === "exception")
                },
                {
                  value: "other",
                  text: "Other (please specify)",
                  checked: (reason === "other"),
                  conditional: {
                    html: otherHtml
                  }
                },
                {
                  divider: "or"
                },
                {
                  value: "no-reason",
                  text: "I do not have an exceptional reason",
                  checked: (reason === "no-reason")
                }
              ]
            }) }}

          {{ govukButton({
            text: "Save and continue"
          }) }}
        </form>
      </div>
    </div>
  </main>
{% endblock %}
